@model Bank2.Models.SharedData
@{
  ViewData["Title"] = "User";
}
<head>
  <link rel="stylesheet" href="~/css/Dashboard.css" />
</head>

<body class="">
  <header class="baseSection">
    <h1 class="removeMargin">Transaction</h1>
  </header>
  <section class="baseSection">
    <h2 class="removeMargin">All Transactions</h2>
    <table>
      <thead>
        <tr>
          <th>A/C No</th>
          <th>Type</th>
          <th>Amount</th>
          <th>Date</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        @foreach (var tran in Model.Transactions)
        {
          if (tran != null && Model.Transactions.Count > 0)
          {
            <tr>
              <td>@tran.AccountNumber</td>
              <td>@tran.Type</td>
              <td>@tran.Amount</td>
              <td>@tran.Date</td>
              <td>@tran.Description</td>
            </tr>
          }
        }
      </tbody>
    </table>

  </section>
</body>
