@{

    if (Context.Session.GetString("LogStatus") == "dash")
    {
        Layout = "CustomLayout";
    }

    if (Context.Session.GetString("LogStatus") == "log")
    {
        Layout = "LoginLayout";
    }
    // Layout = Context.Session.GetString("LogStatus") == "dash" ? "CustomLayout" : "LoginLayout";
    // {
    //     Layout = "CustomLayout";

    // }
    // else
    // {
    //     Layout = "LoginLayout";
    // }
    // Layout = "_Layout";
}
